# pjt-nx

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build pjt-nx` to build the library.

## Running unit tests

Run `nx test pjt-nx` to execute the unit tests via [Jest](https://jestjs.io).
